#!/bin/bash

while true; do
    clear
    echo "╔════════════════════════════════════════════════════════════════════╗"
    echo "║                   SULTAN CHAIN LIVE DASHBOARD                       ║"
    echo "║                      $(date +"%Y-%m-%d %H:%M:%S")                          ║"
    echo "╚════════════════════════════════════════════════════════════════════╝"
    echo ""
    
    # Web Interface Status
    echo "┌─────────────────────────────────────────────────────────────────────┐"
    echo "│ 🌐 WEB INTERFACE                                                    │"
    echo "├─────────────────────────────────────────────────────────────────────┤"
    if pgrep -f "python3 -m http.server 3000" > /dev/null; then
        PID=$(pgrep -f "python3 -m http.server 3000")
        echo "│ Status:     ✅ RUNNING (PID: $PID)                                 │"
        echo "│ Local:      http://localhost:3000                                  │"
        echo "│ External:   https://orange-telegram-pj6qgwgv59jjfrj9j-3000.app.github.dev │"
    else
        echo "│ Status:     ❌ STOPPED                                             │"
    fi
    echo "└─────────────────────────────────────────────────────────────────────┘"
    
    echo ""
    echo "┌─────────────────────────────────────────────────────────────────────┐"
    echo "│ 📊 PROJECT METRICS                                                  │"
    echo "├─────────────────────────────────────────────────────────────────────┤"
    echo "│ Completion:      70% ████████████████████░░░░░░░░                  │"
    echo "│ Gas Fees:        $0.00 (Zero Forever)                              │"
    echo "│ TPS Capability:  1,200,000+                                        │"
    echo "│ Quantum Safe:    ✅ Enabled                                        │"
    echo "│ Staking APY:     13.33%                                            │"
    echo "│ Cosmos IBC:      ✅ Ready                                          │"
    echo "└─────────────────────────────────────────────────────────────────────┘"
    
    echo ""
    echo "┌─────────────────────────────────────────────────────────────────────┐"
    echo "│ 🚀 QUICK ACTIONS                                                    │"
    echo "├─────────────────────────────────────────────────────────────────────┤"
    echo "│ [1] Open Web Interface in Browser                                  │"
    echo "│ [2] View Web Server Logs                                           │"
    echo "│ [3] Check Build Status                                             │"
    echo "│ [4] View Full Documentation                                        │"
    echo "│ [5] Refresh Dashboard                                              │"
    echo "│ [Q] Quit Dashboard                                                 │"
    echo "└─────────────────────────────────────────────────────────────────────┘"
    echo ""
    echo -n "Select action: "
    
    read -t 5 -n 1 action
    case $action in
        1)
            echo ""
            echo "Opening web interface..."
            "$BROWSER" http://localhost:3000
            sleep 2
            ;;
        2)
            echo ""
            echo "Last 10 web server logs:"
            tail -10 /tmp/web.log 2>/dev/null || echo "No logs available"
            echo ""
            echo "Press Enter to continue..."
            read
            ;;
        3)
            echo ""
            echo "Checking build status..."
            if [ -f /workspaces/0xv7/target/debug/sultan_node ]; then
                echo "✅ Debug build exists"
            else
                echo "⏳ Build in progress or pending"
            fi
            echo ""
            echo "Press Enter to continue..."
            read
            ;;
        4)
            echo ""
            cat /workspaces/0xv7/SULTAN_CHAIN_FINAL_STATUS.md 2>/dev/null | head -50
            echo ""
            echo "Press Enter to continue..."
            read
            ;;
        q|Q)
            echo ""
            echo "Exiting dashboard..."
            exit 0
            ;;
    esac
done
